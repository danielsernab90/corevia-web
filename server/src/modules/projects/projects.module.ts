import { Module } from "@nestjs/common";

/**
 * Placeholder — delivery projects linked to won leads.
 * Follow the Leads module pattern when implementing.
 */
@Module({})
export class ProjectsModule {}
