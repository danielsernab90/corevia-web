import { Module } from "@nestjs/common";

/**
 * Placeholder — aggregated dashboard reads for Command Center.
 * Do not put write-side lead logic here — compose from other modules later.
 */
@Module({})
export class DashboardModule {}
